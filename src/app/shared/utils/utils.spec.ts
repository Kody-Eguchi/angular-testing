describe('utils', () => {
  describe('range', () => {
    it('some name of the test', () => {
      expect(1).toEqual(1);
    })
  })

  describe('pluck', () => {
    it('some name of the test', () => {
      expect(1).toEqual(1);
    })
  })
})
